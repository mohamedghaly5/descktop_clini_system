# دليل إصدار وتحديث البرنامج (Release Guide)

هذا الدليل يشرح كيفية بناء وإصدار نسخ جديدة من برنامج **Dental Flow** لكل من الكمبيوتر (Windows) والموبايل (Android).

---

## 1️⃣ الخطوة الأولى: تحديث رقم الإصدار
قبل بناء أي نسخة جديدة، يجب تغيير رقم الإصدار لتمييزها عن السابقة.

1. افتح ملف `package.json`.
2. ابحث عن السطر:
   ```json
   "version": "1.0.6"
   ```
3. قم بتغيير الرقم إلى الإصدار الجديد (مثلاً: `"1.0.7"`).
4. **شغل هذا الأمر** لتحديث النسخة في ملفات الأندرويد والويندوز أيضاً:
   ```powershell
   npx cap sync
   ```

---

## 2️⃣ ثانياً: بناء نسخة الكمبيوتر (Windows Desktop)

لإنشاء ملف التثبيت (`.exe`) ورفعه تلقائياً إلى صفحة الإصدارات على GitHub:

1. افتح التيرمينال (Terminal) في مجلد المشروع.
2. اكتب الأمر التالي:
   ```powershell
   npm run deploy
   ```
3. **ماذا سيحدث؟**
   - سيقوم ببناء البرنامج.
   - سيقوم بإنشاء ملف التثبيت في مجلد `release`.
   - سيقوم برفعه تلقائياً إلى صفحة [Releases](https://github.com/mohamedghaly5/descktop_clini_system/releases) على GitHub.

---

## 3️⃣ ثالثاً: بناء تطبيق الأندرويد (Android APK)

بما أنك لا تملك Android Studio حالياً، فنحن نستخدم سيرفرات GitHub لبناء التطبيق لك مجاناً.

**خطوات الحصول على ملف الـ APK:**

1. بعد الانتهاء من التعديلات وتحديث رقم الإصدار (الخطوة 1).
2. **ارفع الكود إلى GitHub** باستخدام الأوامر التالية بالترتيب:
   ```powershell
   git add .
   git commit -m "Update version to 1.0.7"
   git push origin main
   ```
3. بمجرد انتهاء الرفع، اذهب فوراً إلى هذا الرابط:
   👉 **[صفحة تقدم البناء (GitHub Actions)](https://github.com/mohamedghaly5/descktop_clini_system/actions)**

4. ستجد عملية جديدة بدأت باسم **Build Android APK**.
5. انتظر حتى تتحول العلامة إلى اللون الأخضر (✅ Catch success).
6. اضغط على اسم العملية، ثم انزل إلى أسفل الصفحة ستجد قسماً اسمه **Artifacts**.
7. اضغط على **`app-debug.apk`** لتحميل التطبيق وتثبيته على هاتفك.

---

### 💡 ملاحظة للمستقبل (في حال تثبيت Android Studio)
إذا قمت بتثبيت برنامج Android Studio وتريد البناء من جهازك مباشرة:
1. شغل الأمر: `npx cap open android`
2. انتظر فتح البرنامج، ثم من القائمة اختر: `Build > Build Bundle(s) / APK(s) > Build APK(s)`.
